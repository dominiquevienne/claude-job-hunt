# Board measurement — Job509 (`job509.com`, Haiti): «Emplois en Haiti» — port 443 refuses the connection (the declared client ×2 and curl), plain HTTP serves the site (200, 10 KB, identical twice) whose front says «Aucunes offres disponibles» — an empty board today over HTTP only; rules unreadable (`certain: False`); no adapter yet

<!-- verified: 2026-09-20 -->

<!-- hosts: job509.com -->
<!-- script: none -->
<!-- countries: HT -->
<!-- content: measured · **`https://job509.com/` — `Connection refused` on two reads by the declared client (12:15:35–12:15:42 UTC) and to curl; the name resolves on 1.1.1.1 and 8.8.8.8 (67.23.16.168, NOERROR both); `http://job509.com/` answers 200 ×2 (10 026 B, md5 f83a9614e714 identical): «Job509 | Emplois en Haiti», a search form (secteur, entreprises) and «Aucunes offres disponibles», links `/feed/`, `/emp_signup`, `/events`; no JobPosting; `_robots.allowed('job509.com','/')` → open, `certain: False` (the rules file could not be read over https)** · 2026-09-20 -->
<!-- witness: the front's own «Aucunes offres disponibles» — zero · 2026-09-20 -->

**Named on the Haiti page since the Atlas inventory of 2026-09-04 (#147),
never carded; measured for #771 on 2026-09-20 12:15–12:18 UTC by the declared
client, the guard on the exact path first, `bin/fetch-body.py`, two reads.**
*A measurement, not an adapter.*

The 2026-09-04 note («connexion refusée») was https; the site lives on plain HTTP and publishes nothing today — **a route to nothing is not a coverage** (the 08.09 rule on `skillingpakistan`), and a host that answers is not a silent one.

```
_robots.allowed('job509.com', '/')   open, certain False (https refused)
GET https://job509.com/   Connection refused ×2 (client), curl 000
GET http://job509.com/    200 ×2 — «Aucunes offres disponibles», a /feed/
```

The adapter's first line, the day an ad appears: `/feed/` (an RSS, if it is one) over plain HTTP — which the plugin only does where the site offers nothing else.
