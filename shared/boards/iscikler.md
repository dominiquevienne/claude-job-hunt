# Board measurement — iş-çikler (`iscikler.com`, Northern Cyprus): «Kuzey Kıbrıs'ın İş Arama Portalı» — an 8.5 KB Vite/React shell (`<div id="root">`, one hashed bundle) with no card, count or link in its markup, served to the declared client (identical twice), rules open; the API the shell calls is the adapter's first line; no adapter yet

<!-- verified: 2026-09-18 -->

<!-- hosts: iscikler.com -->
<!-- script: none -->
<!-- countries: CYN -->
<!-- content: measured · **the root (200, 8 557 B, md5 a7b861f7f9a0 identical on two reads) is a client-rendered shell — `/assets/index-Ci11pkKW.js`, `<div id="root">` — with no job card, count, link or JobPosting in its markup; `_robots.allowed('iscikler.com','/')` → open, certain** · 2026-09-18 -->
<!-- witness: none — the shell states nothing · 2026-09-18 -->

**Found by the Northern Cyprus search of #606 (a country never searched),
measured 2026-09-18 07:20–07:22 UTC by the declared client, the guard on the
exact path first, `bin/fetch-body.py`, two reads.** The method is written
on #606: two searches in Turkish naming the Labour Department and the
private boards, no composed host names. *A measurement, not an adapter.*

```
_robots.allowed('iscikler.com', '/')   open
GET https://iscikler.com/   200 ×2 — see the content line
```

The adapter's first line: the API the bundle calls (a browser's network panel), replayed with the page's own parameters, or a browser route.
