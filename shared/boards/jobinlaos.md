# Board measurement — Jobinlaos (`jobinlaos.com`, Laos): «Find Your Dream Job in Laos» — the root served to the declared client (48 KB) is a Nuxt shell whose title still carries its template's name («jobsmerch.com»), the content client-rendered; rules unreadable (no rules, `certain: False`); no adapter yet, the list route to measure

<!-- verified: 2026-09-17 -->

<!-- hosts: jobinlaos.com -->
<!-- script: none -->
<!-- countries: LA -->
<!-- content: measured · **the root (200, 47 897 B, md5 c1ebcdf3b145 / 2662b4c1b80b — a rendered element moves) is a Nuxt UI shell — theme CSS, `/jobs` and `/hot-jobs` links, no card, no count, no JobPosting in its markup; its `<title>` reads «jobsmerch.com - Find Your Dream Job in Laos - jobsmerch.com», a white-label template's name; `_robots.allowed('jobinlaos.com','/')` → open, `certain: False` (the rules file could not be read); whether the route the shell calls serves the client is the adapter's first line** · 2026-09-17 -->
<!-- witness: none — the root is a shell · 2026-09-17 -->

**Found by the Laos search of #602 (a country never searched), measured
2026-09-17 13:49–13:51 UTC by the declared client, the guard on the exact path
first, `bin/fetch-body.py`, two reads, two public resolvers on a DNS
negative.** The method is written on #602: one search naming the country's
boards and agencies (the UN aggregators left aside), no composed host
names. *A measurement, not an adapter.*

```
_robots.allowed('jobinlaos.com', '/')   open, certain False — the rules file did not answer as one
GET https://jobinlaos.com/               200 ×2 — a Nuxt shell, /jobs, /hot-jobs, the template's name in the title
```
